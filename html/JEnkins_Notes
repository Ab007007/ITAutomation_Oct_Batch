Jenkins Notes : 
Installing Docker on CentOS : 
https://docs.docker.com/install/linux/docker-ce/centos/
 ping google.com
  134  sudo yum install -y yum-utils   device-mapper-persistent-data   lvm2
  135  sudo yum-config-manager     --add-repo     https://download.docker.com/linux/centos/docker-ce.repo
  136  sudo yum install docker-ce docker-ce-cli containerd.io
  137  sudo systemctl start docker


Installing Docker Compose : 
https://docs.docker.com/compose/install/
sudo curl -L "https://github.com/docker/compose/releases/download/1.24.1/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose


Install Jenkins Using Docker :
-----------------------------
Docker uses image / snapshot

Docker compose file is a script or its a definintion which contains definition of which servies to start

